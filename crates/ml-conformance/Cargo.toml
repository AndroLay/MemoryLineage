[package]
name = "ml-conformance"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Structured parity report for MemoryLineage Rust lanes"

[dependencies]
ml-core = { path = "../ml-core" }
ml-local-evm = { path = "../ml-local-evm" }
ml-spec-types = { path = "../ml-spec-types" }
ml-verifier-independent = { path = "../ml-verifier-independent" }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
thiserror = "2.0.20"
