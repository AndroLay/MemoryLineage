[package]
name = "ml-portability"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Explicit local portability rehearsals for MemoryLineage execution targets"

[dependencies]
ml-local-evm = { path = "../ml-local-evm" }
ml-spec-types = { path = "../ml-spec-types" }
serde = { version = "1.0.229", features = ["derive"] }
thiserror = "2.0.20"
