[package]
name = "ml-evidence"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Versioned MemoryLineage evidence loading and serialization"

[dependencies]
ml-spec-types = { path = "../ml-spec-types" }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
thiserror = "2.0.20"
